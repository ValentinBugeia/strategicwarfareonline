// Mirrors server/src/game/unitTypes.js for display purposes (cost/label).
// The server is the source of truth and re-validates cost on purchase.
export const UNIT_TYPES = {
  infantry: { label: 'Infanterie', cost: 500 },
};

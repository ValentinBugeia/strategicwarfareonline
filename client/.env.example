# Leave unset in development: the Vite proxy forwards /api and /socket.io
# to the backend on localhost:4000. Only set this in production when the
# API is served from a different origin.
# VITE_API_URL=https://api.example.com
